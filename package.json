{
    "name": "mention-suggestion",
    "version": "1.0.0",
    "description": "A framework agnostic mention and suggestion library for use with contenteditable elements.",
    "main": "lib/index.js",
    "scripts": {
        "build": "rm -rf ./docs/lib && rm -rf ./lib && tsc --project tsconfig.json && cp -r ./lib ./docs/lib && tsc --project tsconfig.docs.json",
        "dev": "tsc  --project tsconfig.json --watch & tsc  --project tsconfig.docs.json --watch & http-server -o ./docs"
    },
    "author": "iambriansreed",
    "keywords": [
        "Abilities",
        "Permissions"
    ],
    "license": "ISC",
    "devDependencies": {
        "http-server": "^14.1.1",
        "typescript": "^5.5.4"
    }
}
